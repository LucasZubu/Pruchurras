import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'silvio.lucas.aula.pruchurras',
  appName: 'pru-churras',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
